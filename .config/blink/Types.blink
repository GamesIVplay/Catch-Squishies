type Id = f64
type Cash = u32
type MoneyMultiplier = u8
type FriendBoost = u32
type LastFoodRestock = u32
type FoodRestockEndTime = u32
type Lasso = string(1..32)
type OwnedLassos = Lasso[0..32]
type CatchId = string(0..64)
type SquishyName = string(0..64)
type SquishyKind = string(0..16)
type SquishyRarity = string(0..32)
type AssetImage = string(0..128)
type CatchMessage = string(0..160)
type InventoryItemId = string(0..80)
type InventoryItemType = string(0..16)
type InventoryItemName = string(0..64)
type InventorySlotName = string(0..32)
type RobuxProductKey = string(1..64)
type Mutation = string(0..32)
type EventName = string(0..32)
type EventSource = string(0..16)
type EventMutation = string(0..32)
type PlotSquishyMoveState = string(0..16)
type IndexKey = string(0..96)
type Index = IndexKey[0..256]
type ClaimedIndexRewards = IndexKey[0..256]
type DailyStart = f64
type ClaimedDailyRewards = u8[0..32]
type TutorialStep = u8
type TutorialSubevent = string(1..64)
type PlaytimeStart = f64
type ClaimedPlaytimeRewards = u8[0..32]

map FoodStock = { [string]: u16 }

enum NotificationType = {Success, Info, Warning, Error, Reward, Announcement}
enum CatchCancelReason = {Invalid, Locked, Lost, Cancelled}
